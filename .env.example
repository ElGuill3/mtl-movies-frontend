# MTL Movies Frontend - Environment Variables

# Backend API URL (IP:port when running on another machine via Tailscale)
# Local development (default):
VITE_API_URL=http://localhost:8080

# Tailscale example (replace XX with actual IP from `tailscale ip -4` on server):
# VITE_API_URL=http://100.84.XX.XX:8080